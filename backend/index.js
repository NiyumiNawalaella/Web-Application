// const { Socket } = require("socket.io");

// let express = require(id = 'express');
// let app = express();

// let http = require(id ='http');
// let server = http.server(app);

// let socketIO = require(id = 'socket.io');


// const port = process.env.port || 3000;

// server.listen(port, () => {
//     console.log(`started on port: ${port}`);
// });

// io.on('connection', (Socket) =>{
//     Socket.on('John', (data) => {
//         Socket.join(data.room);
//         Socket.broadcast.to(data.room).emit(ev, 'user joined');
//     });
//     Socket.on('message', (data) => {
//         io.in(data.room).emit(ev,'new message', args = {user: data.user, message: data.message});
//     });
// });
console.log('Hello World')